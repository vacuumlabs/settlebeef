{
  "name": "contracts",
  "version": "1.0.0",
  "description": "**Foundry is a blazing fast, portable and modular toolkit for Ethereum application development written in Rust.**",
  "main": "index.js",
  "directories": {
    "lib": "lib",
    "test": "test"
  },
  "scripts": {
    "deploy": "bash -c 'source .env && forge script script/Deploy.s.sol:Deploy --rpc-url $RPC_URL --broadcast --private-key $DEPLOYER_PK'",
    "deploy:localhost": "bash -c 'source .env && forge script script/Deploy.s.sol:Deploy --broadcast --private-key $LOCALCHAIN_DEPLOYER_PK --fork-url http://localhost:8545'"
  },
  "author": "",
  "license": "ISC"
}
